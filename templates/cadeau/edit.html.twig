{% extends 'base.html.twig' %}

{% block title %}Edit Cadeau{% endblock %}

{% block body %}
    <h3 class="text-center p-3">Modifier  Les informations du cadeau : {{ cadeau.designation }}</h3>

    {{ include('cadeau/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cadeau_index') }}" class="btn btn-info mx-3 my-3">back to list</a>

    {{ include('cadeau/_delete_form.html.twig') }}
{% endblock %}
