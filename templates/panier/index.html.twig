{% extends 'base.html.twig' %}

{% block title %}Mon Panier{% endblock %}

{% block body %}
    <style>
        .alignerItem {
            display: flex;
            justify-content: space-between;
        }
    </style>

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h4 class="text-center p-4">
                    {% if is_granted("ROLE_ADMIN") or is_granted("ROLE_GESTION") %}
                        Liste des cadeaux souhaités par <strong class="text-warning">{{ panier[0].person }}</strong>
                    {% else %}
                        List de mes cadeaux pour Noel
                    {% endif %}
                </h4>
                {% if panier | length > 0%}
                    <div class="table-responsive">
                        <table id="mytable" class="table table-bordred table-striped">

                            <thead>
                            <th>Image</th>
                            <th>Designation</th>
                            <th>Age</th>
                            <th>Prix</th>
                            <th>User</th>
                            <th>Delete</th>
                            </thead>
                            <tbody>
                            {% set total = null %}
                            {% for item in panier %}
                                <tr>
                                    <td>
                                        <img src="{{ asset("uploads/cadeaux/"~item.cadeau.image) }}" alt=""
                                             class="img-thumbnail img_panier">
                                    </td>
                                    <td>{{ item.cadeau.designation }}</td>
                                    <td>{{ pluriels(item.cadeau.age, 'an') }}</td>
                                    <td>{{ item.cadeau.prix | number_format(2,',','.')}} €</td>
                                    <td>{{ item.person }}</td>
                                    <td><a href="{{ path('panier_delete_from_list', {'id' : item.id}) }}"><button class="btn btn-danger btn-xs" ><i class="fas fa-trash-alt"></i></button></a></td>
                                </tr>
                                {%  set total = item.cadeau.prix + total  %}
                            {% endfor %}
                            </tbody>
                        </table>
                        <table id="mytable" class="table table-bordred table-striped">
                            <thead>
                                <th class="text-primary">Total</th>
                                <th class="text-dark text-right">{{ total | number_format(2,',','.') }} €</th>
                            </thead>
                        </table>
                    </div>
                    <div class="my-3 alignerItem">
                        <a href="
                                    {% if  is_granted("ROLE_ADMIN") %}

                                    {% else %}
                                        {{ path('cadeau_index') }}
                                    {% endif %}" class="btn btn-info text-left">Retour</a>
                        {% if is_granted("ROLE_ADMIN") %}
                            <a href="{{ path('valider_commande', {"id" : panier[0].person.id}) }}" class="btn btn-success text-right">Valider la commande</a>
                        {% endif %}
                    </div>

                    {% else %}
                    <div class="text-center my-5">
                        <h3 class="text-warning">Pas de Liste de cadeaux</h3>
                    </div>
                {% endif %}


            </div>
        </div>
    </div>
{% endblock %}
