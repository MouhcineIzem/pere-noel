{% extends 'base.html.twig' %}

{% block title %}List des commades{% endblock %}

{% block body %}
<div class="d-flex bd-highlight mb-3">

    {% include 'admin/sidebar.html.twig' %}

    <div class="example-wrapper">

        <div class="container text-center p-4">
            <h3>Les commandes     de tous les utilisateures : </h3>
        </div>

        <table class="table">
            <thead class="bg-warning">
            <tr>
                <th scope="col">N° commande</th>
                <th scope="col">Person</th>
                <th scope="col">Etat de Commande</th>
            </tr>
            </thead>
            <tbody class="">
            {% for item in panier %}
            <tr>
                    <td>{{ number }}</td>
                    <td>{{ item.person }}</td>
                {% if item.isValide %}
                    <td class="text-center text-success"><i class="fas fa-check-circle"></i></td>
                {% else %}
                    <td class="text-center text-danger"><i class="fas fa-check-circle"></i></td>
                {% endif %}
            </tr>
                {% set number = number + 1 %}
            {% endfor %}
            </tbody>
        </table>
    </div>

</div>
{% endblock %}
